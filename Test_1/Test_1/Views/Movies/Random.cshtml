@model Test_1.Models.MovieRentals

@{
    ViewBag.Title = "Random";
}

<h2>Film:@Model.movie.Name</h2>
<h2>Film:@Model.movie.Rating</h2>
Download from <a href="@Model.movie.DownloadURL">here</a>
<h3>Clients:</h3>

<ul>
    @{ 
        int i = 0;
        foreach(var c in Model.customers)
        {
            @Html.ActionLink(c.Name, "ShowClient","Movies")  // neznam kako da prenesam parametar vaka 
            //<li><a href="ShowClient/@i">@c.Name</a></li>  
            i++;
        }
    }
</ul>

