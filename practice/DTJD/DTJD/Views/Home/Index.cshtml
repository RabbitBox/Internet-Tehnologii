@model List<DTJD.Models.Drink>

@{
    ViewBag.Title = "Home Page";
}

@Html.Partial("Carousel")

<h2>Prefered drinks of the week</h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var drink in Model)
    {
        @Html.Partial("_ShowDrinks", drink)
    }
</div>